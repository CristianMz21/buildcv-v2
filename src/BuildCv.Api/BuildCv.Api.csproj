<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.AspNetCore.Authentication.JwtBearer" Version="10.0.11" />
    <PackageReference Include="Microsoft.AspNetCore.OpenApi" Version="10.0.10" />
    <PackageReference Include="Microsoft.OpenApi" Version="2.11.0" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\BuildCv.Application\BuildCv.Application.csproj" />
    <ProjectReference Include="..\BuildCv.Infrastructure\BuildCv.Infrastructure.csproj" />
  </ItemGroup>

  <!-- Matches BuildCv.Infrastructure. ProjectedList is an internal wire-mapping detail whose only
       interesting property (Count projects nothing) cannot be observed from outside the assembly. -->
  <ItemGroup>
    <InternalsVisibleTo Include="BuildCv.Api.Tests" />
  </ItemGroup>

</Project>
